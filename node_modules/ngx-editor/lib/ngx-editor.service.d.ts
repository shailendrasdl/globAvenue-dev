import { Schema } from 'prosemirror-model';
import { Plugin } from 'prosemirror-state';
import { NgxEditorConfig, NodeViews } from './types';
export declare class NgxEditorServiceConfig {
    plugins: Plugin[];
    nodeViews: NodeViews;
    schema: Schema;
}
export declare class NgxEditorService {
    config: NgxEditorServiceConfig;
    constructor(config?: NgxEditorServiceConfig);
}
export declare function provideMyServiceOptions(config?: NgxEditorConfig): NgxEditorConfig;
