import { EditorState, Transaction } from 'prosemirror-state';
import { NodeType } from 'prosemirror-model';
export declare const toggleList: (type: NodeType<any>, itemType: NodeType<any>) => (state: EditorState<any>, dispatch: (tr: Transaction<any>) => void) => boolean;
