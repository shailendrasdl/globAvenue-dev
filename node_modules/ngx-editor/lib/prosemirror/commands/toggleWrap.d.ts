import { EditorState, Transaction } from 'prosemirror-state';
import { NodeType } from 'prosemirror-model';
export declare const toggleWrap: (type: NodeType<any>) => (state: EditorState<any>, dispatch: (tr: Transaction<any>) => void) => boolean;
