import { EditorState, Transaction } from 'prosemirror-state';
import { NodeType } from 'prosemirror-model';
export declare const toggleBlockType: (type: NodeType<any>, toggleType: NodeType<any>, attrs?: {}) => (state: EditorState<any>, dispatch: (tr: Transaction<any>) => void) => boolean;
